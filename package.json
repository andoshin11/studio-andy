{
  "private": true,
  "license": "MIT",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "lint": "lerna run lint",
    "test": "lerna run test",
    "screenshot": "cd packages/studio-andy-storybook; npm run screenshot",
    "reg-suit": "cd packages/studio-andy-storybook; npm run reg-suit"
  },
  "devDependencies": {
    "eslint-config-prettier": "^4.0.0",
    "eslint-config-typescript": "^1.1.0",
    "lerna": "^3.10.7"
  }
}
